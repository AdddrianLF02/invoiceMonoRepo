{
  "name": "@repo/infrastructure",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "generate": "prisma generate",
    "clean": "rimraf dist",
    
    "db:migrate": "dotenv -e ../../apps/backend/.env -- prisma migrate dev --schema ./src/database/schema/schema.prisma",
    "db:push": "dotenv -e ../../apps/backend/.env -- prisma db push --schema ./src/database/schema/schema.prisma"
  },
  "prisma": {
    "schema": "src/database/schema/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.16.3",
    "@repo/core": "*"
  },
  "devDependencies": {
    "@nestjs/common": "^11.0.1",
    "@repo/typescript-config": "*",
    "prisma": "^6.16.3",
    "typescript": "^5.7.3",
    "rimraf": "^6.0.1",
    "dotenv-cli": "^7.4.2" 
  },
  "peerDependencies": {
    "@nestjs/common": "^11.0.1"
  }
}
