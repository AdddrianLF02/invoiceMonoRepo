"use client";
import React from "react";

export default function CustomersPanel({ accessToken }: { accessToken: string }) {
    return (
        <div className="space-y-6">
            <h1 className="text-2xl font-bold">Clientes</h1>
            <p className="text-muted-foreground">Panel de clientes — por implementar</p>
        </div>
    );
}
